@model EditBranch
@* <div class="container">
    <h2>Edit Branch</h2>
    <form method="post">
        <div class="form-group">
            <label for="Name">Branch Name:</label>
            <input type="text" class="form-control" id="Name" name="LocationName" value="@Model.LocationName">
        </div>
        <div class="form-group">
            <label for="Location">Location:</label>
            <input type="text" class="form-control" id="Location" name="LocationURL" value="@Model.LocationURL">
        </div>
        <div class="form-group">
            <label for="BranchManager">Branch Manager:</label>
            <input type="text" class="form-control" id="BranchManager" name="branchManager" value="@Model.BranchManager">
        </div>
        <div class="form-group">
            <label for="EmployeeCount">Employee Count:</label>
            <input type="number" class="form-control" id="EmployeeCount" name="employeeCount" value="@Model.EmployeeCount">
        </div>
        <button type="submit" class="btn btn-primary">Save</button>
        <a class="btn btn-secondary" asp-action="Index">Cancel</a>
    </form>
</div> *@

@using (Html.BeginForm("Edit", "Bank", FormMethod.Post))
{
    @Html.EditorForModel()
    <button type="submit">Edit</button>
}